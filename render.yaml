services:
  - type: web
    name: mikana-relay
    env: node
    rootDir: server
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 3005
      - key: NODE_ENV
        value: production
